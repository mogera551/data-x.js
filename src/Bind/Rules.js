
/**
 * --bind-text-content: property;
 * --bind-value: property;
 * --bind-style--display: property;
 * --bind-class-selected: property;
 * --bind-radio: property;
 * --bind-checkbox: property;
 * --loop: property;
 * --events: event1,event2;
 */
